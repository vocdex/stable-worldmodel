defaults:
  - _self_

hydra:
  output_subdir: null
  run:
    dir: .

cache_dir: null
env_type: single

world:
  num_envs: 10
  max_episode_steps: 200
  image_shape: [256, 256]

seed: 3072
num_traj: 10000
policy_type: markov_oracle

options: null
